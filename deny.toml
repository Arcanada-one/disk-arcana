# cargo-deny configuration — license allowlist, advisory & ban rules.
# Source: https://embarkstudios.github.io/cargo-deny/

[graph]
all-features = true

[licenses]
# Permissive + weak-copyleft licenses we accept.
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MPL-2.0",
    "Unicode-DFS-2016",
    "Unicode-3.0",
    "Zlib",
    "CC0-1.0",
    "OpenSSL",
]
confidence-threshold = 0.8

[bans]
multiple-versions = "warn"
wildcards = "warn"

[advisories]
yanked = "warn"
# See `.audit.toml` for full rationale.
ignore = [
    "RUSTSEC-2023-0071",  # rsa Marvin — only via sqlx-mysql, never enabled.
    "RUSTSEC-2025-0134",  # rustls-pemfile unmaintained — drops with tonic upgrade.
]

[sources]
unknown-registry = "warn"
unknown-git      = "warn"
