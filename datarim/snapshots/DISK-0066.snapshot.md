---
task_id: DISK-0066
artifact: stage-snapshot
schema_version: 1
stage: auto
command: /dr-auto
captured_at: 2026-07-31T11:05:00Z
captured_by: agent
recommended_next: /dr-archive DISK-0066
options:
  - /dr-archive DISK-0066 — recommended — implementation verified, PR #128 green, merged to main
  - /dr-status — review full backlog
---

# DISK-0066 — stage snapshot

**Scope:** ARCA-0194 R6-17 — remediate vulnerable, unmaintained and yanked
dependencies without weakening cargo-audit / cargo-deny policy.

## State

Implemented and verified on `sec/DISK-0066-dependency-remediation` (PR #128).

All three in-scope advisories were closed by real dependency changes, not
suppressions:

- `quick-xml` 0.39.4 → 0.41.0 via `wayland-scanner` 0.31.11 (RUSTSEC-2026-0194/0195)
- `ttf-parser` 0.25.1 removed entirely via `eframe` 0.29 → 0.34, whose epaint
  replaced `ab_glyph` with **skrifa** (RUSTSEC-2026-0192)
- yanked `spin` 0.9.8 → 0.9.9

`deny.toml`'s advisory ignore list is empty and `yanked` is now `"deny"` — a
hard gate, since under the previous `"warn"` neither tool actually failed on a
yanked crate.
The dead root `.audit.toml` (never read by cargo-audit) was deleted and
`.cargo/audit.toml` made authoritative; CI now runs bare `cargo audit` plus the
full `cargo deny check`.

Only `RUSTSEC-2023-0071` (`rsa`, via the never-enabled `sqlx-mysql`) remains
suppressed — no upstream fix exists — now with an exploitability argument and a
retirement condition (sqlx 0.9, Dependabot #34).

## Verification

`cargo audit` 0 findings · `cargo deny check` all four checks ok · `cargo fmt`
· `cargo clippy --all-targets --all-features -D warnings` · `cargo test
--workspace --all-features` 903 tests · macOS GUI typecheck for
`aarch64-apple-darwin` via cargo-zigbuild — all exit 0.

## Notes

Dispatched concurrently to a second agent (Cursor) which had opened PR #128
using the prohibited suppression approach and left a `dead_code` CI failure.
Its `mdns-sd` 0.13 → 0.20 upgrade was preserved; this work was layered on top
and fast-forwarded onto the same branch, so PR #128 stayed the single PR.

## Follow-ups

- sqlx 0.9 (Dependabot #34) — retires the last suppression.
- eframe 0.35 — optional; 0.34 already carried the skrifa switch this task needed.
- Visual QA of the GUI on real macOS hardware (migration was typechecked, not run).
- Compile `disk-gui` in CI via cargo-zigbuild — nothing builds it today.
- Prune dead `deny.toml` allowlist entries (`OpenSSL`, `Unicode-DFS-2016`).
