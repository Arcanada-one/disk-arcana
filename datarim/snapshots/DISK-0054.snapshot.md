---
task_id: DISK-0054
artifact: stage-snapshot
schema_version: 1
stage: do
command: /dr-do
captured_at: 2026-06-15T11:22:00Z
captured_by: agent
recommended_next: /dr-qa DISK-0054
options:
  - /dr-qa DISK-0054 — recommended — L3 requires QA layer verification (PRD V-AC alignment + plan conformance)
  - /dr-status — review full backlog
size_bytes: 2500
truncated: false
---
## /dr-do DISK-0054 round 2 — icns + bundle

### Icon (V-AC-3)
- `assets/disk-arcana.icns`: **255 KB**, `pixelWidth: 1024`, 10 resolution entries (16/32/128/256/512 @1x + @2x), `file` type `ic12`. Real multi-resolution ICNS, not a stub.
- Generation script: `scripts/gen-placeholder-icon.sh` (ImageMagick + iconutil, committed)
- Documented as placeholder: `crates/disk-gui/assets/README.md`

### Bundle (V-AC-2)
- `cargo-bundle v0.11.0` installed via `cargo install cargo-bundle`
- Bundle produced: `target/release/bundle/osx/Disk Arcana.app`
- Binary: `Contents/MacOS/disk-gui` — Mach-O 64-bit arm64, 6.1 MB
- Icon in bundle: `Contents/Resources/disk-arcana.icns` — 255 KB, pixelWidth 1024
- `CFBundleIconFile = disk-arcana.icns` — confirmed via PlistBuddy
- `plutil -lint Contents/Info.plist` — **OK**
- DMG also produced: `target/release/bundle/dmg/Disk Arcana.dmg`
- Quirk documented: cargo-bundle 0.11 with `-p` from workspace root resolves icon paths from cwd not package root. Fix: run from `crates/disk-gui/`. Wrapper script: `scripts/bundle-macos.sh`

### Automated gates (round 2)
- `cargo build -p disk-gui` — **PASS**
- `cargo test -p disk-gui` — **PASS** (23 tests, 3 suites)
- `cargo clippy -p disk-gui --all-targets -- -D warnings` — **PASS** (no issues)
- `cargo fmt --all -- --check` — **PASS**
- `cargo test --workspace` — **PASS** (587 passed, 1 ignored, 59 suites)
- no-bind/listen grep on disk-gui/src/ — **PASS** (0 matches)

### V-AC status
| V-AC | Description | Status |
|------|-------------|--------|
| V-AC-1 | `cargo build -p disk-gui --release` exits 0 | Automated: PASS (via bundle --release) |
| V-AC-2 | `cargo bundle --release` produces Disk Arcana.app | Automated: PASS — .app at target/release/bundle/osx/ |
| V-AC-3 | `.icns` in bundle Resources, icon non-empty | Automated: PASS — 255K real icns confirmed in Resources/ |
| V-AC-4 | Window renders daemon status, degrades gracefully | Manual (operator): open .app, confirm window renders |
| V-AC-5 | Settings panel renders | Manual (operator): confirm settings panel in .app |
| V-AC-6 | No bind()/listen() in crate | Automated: PASS |
| V-AC-7 | CI green with disk-gui added | Automated: workspace PASS |

### Operator manual smoke remaining
1. Open `Disk Arcana.app` in Finder (or `open "target/release/bundle/osx/Disk Arcana.app"`)
2. Confirm cat-face icon appears in Dock (V-AC-3 visual)
3. Confirm main window renders with daemon status (V-AC-4)
4. Confirm settings panel opens and shows port field (V-AC-5)

### Branch
`feat/disk-0054-macos-gui` in `Projects/Disk Arcana/code/disk-arcana/`. Not pushed (operator-gated).
