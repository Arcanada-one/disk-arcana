{
  "name": "disk-arcana-obsidian",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian interface for the local Disk Arcana daemon",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:integration": "DISK_PLUGIN_INTEGRATION=1 vitest run test/daemon-integration.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
