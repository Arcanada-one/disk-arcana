# Deploy examples (DISK-0011)

| File | Purpose |
|------|---------|
| `dreamer-wiki.disk.toml` | Sample `[[share]]` + filter excludes for KB `wiki/` sync |

Copy sections into enrolled nodes' `disk.toml` after adjusting paths.

Runbook: `docs/runbooks/DISK-RB-009-dreamer-wiki-sync.md`
