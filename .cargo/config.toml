# Native cross-compile for aarch64 on x86_64 Linux hosts (CI build matrix).
# Replaces cross-rs/docker when self-hosted runners lack docker.sock (infra-0308).
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
