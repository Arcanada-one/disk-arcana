# DISK-0065 mandate

The code already landed: PR #129 is merged to main. What is missing is closure -
there is no archive document, no QA report and no compliance report, so the task
still shows in_progress on main.

Do this:
1. Verify the merged work actually holds on exact current main - the Windows CI
   Test-step regression and the client/server watcher-exhaustion durability.
   Re-run the real suites; do not trust the merge as proof.
2. Run QA, then compliance, then archive, producing the real artifacts.
3. Land the archive through a PR and remove the stale active-task rows.

If you find the merged fix is incomplete, fix it rather than archiving a false
pass.

## Autonomy (operator-granted, ARCA-0194 epic)

Full autonomy. Do NOT ask the operator anything. Resolve every blocker and
design disagreement yourself: consilium for contested calls, web research for
unknowns (record in `datarim/insights/`). Production is authorised: merge to
main, deploy, restart services.

Hard boundaries (safety, not operator gates):
- Never print, echo, log, commit or transcribe a real credential value.
- Do NOT fund the DeepSeek account. The exposed key still authenticates
  (`/models` -> 200) and is inert only because the balance is exhausted;
  topping up re-arms the leak. If coworker delegation fails on exhausted
  balance, use the native fallback - that is authorised.
- Prove what you claim. An unproven criterion is BLOCKED, never N/A, and never
  closed on a ticket alone.

Work to completion: finish, commit, push, PR, green CI, merge, then QA,
compliance and archive.

