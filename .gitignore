# Rust
/target
**/target
Cargo.lock.bak

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Local config / secrets
.env
.env.local
*.local.toml
disk.toml

# Local DB / temp data
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Logs / coverage
*.log
*.profraw
tarpaulin-report.html
cobertura.xml

# Byproduct of `cargo-zigbuild`, used for the out-of-band macOS typecheck of
# crates/disk-gui (see SECURITY.md). Created in the working directory on every
# run; never an input to the build.
.intentionally-empty-file.o
